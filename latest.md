### 新增功能
1. **历史记录弹框显示** - 优化历史记录支持弹框显示，方便查看和管理历史连接记录
2. **跨平台构建支持** - 新增跨平台构建工作流（Windows/Linux/macOS），支持 Ubuntu 24 兼容构建（Electron 35），发布流程支持 macOS 双架构（arm64 + x64）

### 优化修复
1. **复制文本截断修复** - 从输出中复制时自动替换 `\0` 字符，避免复制内容被截断
2. **编码切换修复** - 修复切换 GB2312 编码后日志不显示的问题
3. **固定滚屏优化** - 点击按钮滚动到最底部时自动取消屏幕固定，交互体验更流畅
4. **虚拟串口修复** - 修复 Windows 下虚拟串口不显示的问题，串口列表按平台过滤虚拟端口
5. **跨平台兼容修复** - 修复 Linux 打包的 chrome-sandbox 权限与串口原生模块加载问题，修复测试跨平台兼容性
6. **网络请求优化** - 禁止自动发起网络请求，提升隐私安全性
7. **Ubuntu 构建修复** - Ubuntu 24 构建改用 package-lock-only + npm ci，避免 npm optional deps bug
